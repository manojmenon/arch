apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project-data-0001
  namespace: project-management
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage

